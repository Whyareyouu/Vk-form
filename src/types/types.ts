export type TAction = {
	type: string;
	payload: string;
};
export type TState = {
	tower: string;
	floor: string;
	meetingroom: string;
	date: string;
	comment: string;
};
export type TOption = {
	value: string;
	label: string;
};
